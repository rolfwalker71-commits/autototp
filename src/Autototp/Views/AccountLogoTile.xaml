<UserControl x:Class="Autototp.Views.AccountLogoTile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Border Style="{StaticResource LogoTile}"
                Visibility="{Binding HasLogo, Converter={StaticResource InverseBoolToVis}}">
            <TextBlock Text="{Binding LogoInitial}"
                       Foreground="{DynamicResource LogoTileTextBrush}"
                       FontWeight="SemiBold"
                       FontSize="15"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center" />
        </Border>
        <Border CornerRadius="6"
                Visibility="{Binding HasLogo, Converter={StaticResource BoolToVis}}">
            <Border.Background>
                <ImageBrush ImageSource="{Binding LogoImage}" Stretch="UniformToFill" />
            </Border.Background>
        </Border>
    </Grid>
</UserControl>
