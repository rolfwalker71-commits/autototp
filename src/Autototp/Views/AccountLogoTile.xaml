<UserControl x:Class="Autototp.Views.AccountLogoTile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             RenderOptions.BitmapScalingMode="HighQuality">
    <Grid>
        <Image Source="pack://application:,,,/Assets/default-logo.png"
               Stretch="Uniform"
               Visibility="{Binding HasCustomLogo, Converter={StaticResource InverseBoolToVis}}" />
        <Border CornerRadius="6"
                ClipToBounds="True"
                Visibility="{Binding HasCustomLogo, Converter={StaticResource BoolToVis}}">
            <Border.Background>
                <ImageBrush ImageSource="{Binding LogoImage}" Stretch="UniformToFill" />
            </Border.Background>
        </Border>
    </Grid>
</UserControl>
